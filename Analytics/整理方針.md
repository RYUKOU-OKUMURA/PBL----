# Analytics フォルダ整理方針

**作成日:** 2026-05-21  
**目的:** 分析データの保管・参照を整理する。定期取得は CSV を正とし、可視化は Cursor Canvas を主とする。

---

## 1. 基本方針

| 原則 | 内容 |
|------|------|
| **正（ソース）** | CSV（再取得・再分析可能な生データ） |
| **ビュー（表示）** | Cursor Canvas（分析時の主手段） |
| **ビュー（任意）** | HTML（`generate_report_html.py` でオンデマンド生成。手書きしない） |
| **要約（任意）** | `summary.md`（Cursor 等で書く読み物） |
| **用途で分ける** | 定期取得 `periodic/` と特派分析 `projects/` を混在させない |
| **ルート直下** | README・方針・横断 index のみ。CSV / スクリプトは置かない |

---

## 2. フォルダ構成（整理後）

```
Analytics/
├── README.md                 # セットアップ・実行コマンド（既存を更新）
├── 整理方針.md               # 本ドキュメント
├── index.html                # 横断ダッシュボード（Phase 2 で自動生成）
│
├── scripts/                  # 分析用 Python
│   ├── ga4_traffic_trend_analysis.py
│   ├── search_growth_barrier_analysis.py
│   ├── tv_redcode_spike_deep_analysis.py
│   ├── build_tv_impact_excel.py
│   └── generate_report_html.py   # 新規：CSV → HTML
│
├── periodic/                 # 定期取得（2週間ごとなど）
│   └── YYYY-MM-DD_YYYY-MM-DD/
│       ├── ga4_wp_gsc_analysis.csv
│       ├── ga4_wp_gsc_analysis_queries.csv
│       ├── index.html              # 期間レポート（自動生成）
│       └── summary.md              # 任意：人間向け要約
│
└── projects/                 # 一度きりの深掘り（アーカイブ）
    └── YYYY-MM_テーマ/
        ├── README.md または index.html
        └── （分析 CSV / xlsx / md）
```

**プロジェクトルート**（変更なし）

- `analyze_content_seo.py` … メインの定期取得スクリプト（ここに置いたまま）

---

## 3. 命名ルール

### periodic（定期）

- **フォルダ名:** `YYYY-MM-DD_YYYY-MM-DD`（集計開始日_終了日）
- **例:** `2026-05-08_2026-05-21`
- `last_2weeks_2026-05-21` のような「期間の長さ＋終了日」は使わない

### projects（特派）

- **フォルダ名:** `YYYY-MM_テーマ`（英小文字・ハイフン）
- **例:** `2026-03_tv-redcode` / `2026-03_search-barrier`

### ファイル名（periodic 内）

| ファイル | 役割 |
|----------|------|
| `ga4_wp_gsc_analysis.csv` | 記事別 PV・セッション・GSC（既存のまま） |
| `ga4_wp_gsc_analysis_queries.csv` | クエリ別（既存のまま） |
| `index.html` | 自動生成レポート |
| `summary.md` | 任意。AI / 人間が書く解説 |

---

## 4. 既存データの移行マップ

| 現在 | 移行先 |
|------|--------|
| `last_2weeks_2026-05-21/` | `periodic/2026-05-08_2026-05-21/` |
| `last_3weeks_2026-04-06/` | `periodic/2026-03-17_2026-04-06/` |
| `last_3weeks_.../アクセスレポート_*.md` | 同上フォルダ内 `summary.md` にリネーム |
| `last_14days_2026-03-21/` | `periodic/2026-03-08_2026-03-21/` ※期間要確認 |
| `tv_redcode_deep_2026-03-21/` + TV 関連 xlsx/md | `projects/2026-03_tv-redcode/` |
| `search_barrier_2026-03-21/` | `projects/2026-03_search-barrier/` |
| ルート `ga4_wp_gsc_analysis*.csv` | 該当 periodic へ移すか削除（重複） |
| ルート `*.py` | `scripts/` へ |
| `__pycache__/` | 削除（`.gitignore` に追加） |

---

## 5. HTML 自動生成（やる方向・仕様）

### 5.1 方針

- HTML は **手書きしない**。`generate_report_html.py` が CSV を読んで出力する。
- 外部 CDN 可（Chart.js 等）。**単一 HTML ファイル**でブラウザ打开可能にする。
- データの正は CSV。HTML は再生成可能なビュー。

### 5.2 生成タイミング

1. **定期取得（デフォルト）**  
   `run_periodic.sh` / `analyze_content_seo.py` は **CSV のみ** 出力。HTML は生成しない。
2. **HTML（任意）**  
   `--html` フラグ、または `generate_report_html.py` を手動実行したときのみ生成。
3. **横断ダッシュボード**  
   `generate_report_html.py --index` 実行時に `Analytics/index.html` を更新。

### 5.3 期間レポート `index.html` の内容（Phase 1）

**ヘッダー**

- サイト名・集計期間・生成日時

**サマリーカード**

- 合計 PV / セッション / エンゲージドセッション
- GSC クリック / インプレッション / CTR
- （取得できる場合）前期比 % ※GA4 日次 API を別途呼ぶ or 手入力はしない

**表**

- 記事 TOP10（タイトル・PV・セッション・GSC クリック）
- 検索クエリ TOP10（クリック・インプレッション・平均順位）

**グラフ（Chart.js）**

- 日次セッション折れ線 … `ga4_traffic_trend_analysis.py` と連携、または GA4 API を HTML 生成時に取得

**フッター**

- データソース明記（GA4 / GSC / WordPress）
- 同フォルダ CSV への相対リンク

### 5.4 横断ダッシュボード `Analytics/index.html`（Phase 2）

- `periodic/` 一覧（期間・主要 KPI）
- 直近 3〜6 期間のセッション推移グラフ
- 各期間 `index.html` へのリンク

### 5.5 projects 向け HTML

- 既存 md（`README_analysis.md` 等）は **Phase 1 では移動のみ**。
- 必要なら Phase 3 で TV 分析などを `index.html` 化（優先度低）。

---

## 6. 運用フロー（整理後）

### 定期取得（2週間ごと）

```bash
# 推奨: 直近14日を自動計算して CSV のみ取得
bash Analytics/scripts/run_periodic.sh

# 終了日指定・ dry-run
bash Analytics/scripts/run_periodic.sh --end 2026-05-21
bash Analytics/scripts/run_periodic.sh --dry-run
```

手動で期間を指定する場合:

```bash
START=2026-05-08
END=2026-05-21
python analyze_content_seo.py --start "$START" --end "$END"
```

数値の確認・解説は Cursor で CSV を渡して Canvas 表示する。

### HTML 生成（任意）

```bash
# 横断ダッシュボード（動向分析付き）
python Analytics/scripts/generate_report_html.py --index

# projects HTML 化
python Analytics/scripts/generate_report_html.py --all-projects
```

### トラフィック推移だけ見たいとき

```bash
python Analytics/scripts/ga4_traffic_trend_analysis.py
```

---

## 7. 実装フェーズ

| Phase | 内容 | 状態 |
|-------|------|------|
| **1** | フォルダ移行（periodic / projects / scripts） | 完了 |
| **2** | 期間 HTML 自動生成 | 完了 |
| **3** | 横断 `Analytics/index.html` | 完了 |
| **4** | 既存 periodic HTML バックフィル | 完了 |
| **5** | projects の HTML 化 | 完了 |
| **A** | `run_periodic.sh`（2週間定期取得） | 完了 |
| **B** | index 動向分析強化 | 完了 |

**Looker Studio:** 見送り（Python + CSV で運用。HTML はオンデマンド）

---

## 8. 保持・削除ルール

| 種類 | ルール |
|------|--------|
| `periodic/` | 直近 12 ヶ月は残す。それ以前はアーカイブ or 削除を検討 |
| `projects/` | 基本削除しない（参照資料） |
| ルート直下 CSV | 作らない（デフォルト出力先を `periodic/` に変更検討） |

---

## 9. やらないこと

- HTML を正データにしない（CSV を消さない）
- 手作業で HTML をメンテする
- 週次サブフォルダ（`week1/` `week2/`）まで細分化する
- 最初から Looker Studio 等と二重管理する

---

## 10. 次のアクション（実装時チェックリスト）

- [x] `periodic/` `projects/` `scripts/` 作成
- [x] 既存フォルダを移行マップどおり移動
- [x] ルート `ga4_wp_gsc_analysis*.csv` 整理
- [x] `scripts/` 移動に伴う import パス・README のパス修正
- [x] `.gitignore` に `Analytics/__pycache__/` 追加
- [x] `generate_report_html.py` 実装
- [x] 最新 periodic に HTML 生成テスト
- [x] `README.md` を本方針に合わせて更新
