# AI組織 クイック起動ガイド

tmuxとClaude Codeを使ったマルチエージェントAI組織の起動手順

---

## 🚀 クイック起動（3ステップ）

**前提**: tmux、Claude Code CLIがインストール済み

### Step 1: tmuxセッション作成

```bash
cd ai-team
./setup.sh
```

### Step 2: PRESIDENT起動（本物Claude Opus）

```bash
# 社長画面を開く
tmux attach-session -t president

# 本物Opusで起動
ANTHROPIC_BASE_URL= ANTHROPIC_AUTH_TOKEN= claude --dangerously-skip-permissions
```

> 💡 初回のみブラウザ認証が必要。「Claude is ready!」が出たらOK

**画面から抜ける**: `Ctrl+B` → `D`

### Step 3: 部下たち起動（GLM）

```bash
for i in {0..3}; do
  tmux send-keys -t multiagent:0.$i 'claude --dangerously-skip-permissions' C-m
done
```

### 🎉 起動完了！

| エージェント | モデル | 役割 |
|-------------|--------|------|
| PRESIDENT | 本物Claude Opus | 統括・意思決定 |
| boss1 | GLM-4.7 | チームリーダー |
| worker1,2,3 | GLM-4.7 | 実行担当 |

---

## 📺 画面の確認方法

```bash
# 社長画面
tmux attach-session -t president

# 部下4画面（4分割）
tmux attach-session -t multiagent
```

**画面から抜ける**: `Ctrl+B` → `D`

---

## 💬 PRESIDENTへの指示例

社長画面で入力：

```
あなたはpresidentです。指示書(instructions/president.md)に従って、
○○○を作成してください。
```

---

## ⚡ もっと簡単に起動したい場合（エイリアス設定）

毎回長いコマンドを打つのが面倒な場合、エイリアスを設定しておくと便利。

### 一度だけ実行（設定追加）

```bash
cat >> ~/.zshrc << 'EOF'

# AI組織用エイリアス
alias cc-president='ANTHROPIC_BASE_URL= ANTHROPIC_AUTH_TOKEN= claude --dangerously-skip-permissions'
EOF

source ~/.zshrc
```

### 以降の起動方法

```bash
# 社長画面で
tmux attach-session -t president

# エイリアスで起動
cc-president
```

---

## 🔄 終了・リセット

### 一時停止（セッションを残す）

```bash
# 画面から抜けるだけ
Ctrl+B → D
```

### 完全終了

```bash
tmux kill-server
```

### リセット（最初からやり直し）

```bash
tmux kill-server
rm -rf ./tmp/*
./setup.sh
```

---

---

# 📖 詳細ガイド

以下は詳細な説明やトラブル時の参照用です。

---

## システム概要

### AI組織の構成

```
┌─────────────────────────────────────────────────────┐
│                   Human User                         │
└───────────────────┬─────────────────────────────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │   PRESIDENT Session   │ ← 本物Claude Opus
        │   プロジェクト統括責任者  │
        └───────────┬───────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │    boss1              │ ← GLM-4.7
        │    チームリーダー       │
        └───┬───────┬───────┬───┘
            │       │       │
        ┌───▼───┐ ┌──▼───┐ ┌─▼─────┐
        │worker1│ │worker2│ │worker3│ ← GLM-4.7
        │実行担当A│ │実行担当B│ │実行担当C│
        └───────┘ └───────┘ └───────┘
```

### なぜこの構成？

| 役割 | モデル | 理由 |
|------|--------|------|
| PRESIDENT | 本物Opus | 高度な判断・戦略立案が必要 |
| boss1, workers | GLM | 並列作業でコスト効率を重視 |

---

## セッション構成

| セッション | ペイン | 色 | 役割 |
|-----------|--------|-----|------|
| president | - | 紫 | 統括責任者 |
| multiagent:0.0 | boss1 | 赤 | チームリーダー |
| multiagent:0.1 | worker1 | 青 | 実行担当A |
| multiagent:0.2 | worker2 | 青 | 実行担当B |
| multiagent:0.3 | worker3 | 青 | 実行担当C |

---

## 使い方

### 基本的な指示の流れ

```
Human → PRESIDENT → boss1 → worker1,2,3 → boss1 → PRESIDENT → Human
```

### agent-send.shで指示を送る

```bash
# 社長に送る
./agent-send.sh president "あなたはpresidentです。プロジェクトを開始してください"

# マネージャーに送る
./agent-send.sh boss1 "あなたはboss1です。タスクを分解してください"

# ワーカーに送る
./agent-send.sh worker1 "UIを作成してください"
./agent-send.sh worker2 "ロジックを実装してください"
./agent-send.sh worker3 "テストを作成してください"
```

### エージェント一覧の確認

```bash
./agent-send.sh --list
```

---

## プロジェクトごとのカスタマイズ

### 役割の編集

`instructions/`フォルダ内のファイルを編集：

| ファイル | 役割 |
|----------|------|
| president.md | 社長の指示書 |
| boss.md | マネージャーの指示書 |
| worker.md | ワーカーの指示書 |

### 例：ECサイト開発プロジェクト

**instructions/president.md**
```markdown
# あなたの役割
ECサイト開発プロジェクトの統括責任者として、
ユーザー体験を最優先に意思決定してください。
```

**instructions/worker.md（worker1用にコピー）**
```markdown
# あなたの役割
フロントエンド担当として、商品一覧・カート・決済UIを実装してください。
```

---

## 他プロジェクトへの横展開

### 方法1: ai-teamフォルダをコピー

```bash
# 新プロジェクトにコピー
cp -r ai-team /path/to/new-project/

# 新プロジェクトで起動
cd /path/to/new-project/ai-team
./setup.sh
```

### 方法2: export-to-project.shを使用

```bash
./export-to-project.sh /path/to/new-project my-new-team
```

---

## トラブルシューティング

### Q: 「tmux: command not found」

```bash
# Mac
brew install tmux

# Ubuntu
sudo apt install tmux
```

### Q: 「permission denied」

```bash
chmod +x setup.sh agent-send.sh launch-agents.sh
```

### Q: セッションが見つからない

```bash
# 確認
tmux list-sessions

# 再作成
./setup.sh
```

### Q: PRESIDENTがGLMで動いてしまう

社長起動時に環境変数を指定し忘れている可能性：

```bash
# ❌ これだとGLM
claude --dangerously-skip-permissions

# ✅ これで本物Opus
ANTHROPIC_BASE_URL= ANTHROPIC_AUTH_TOKEN= claude --dangerously-skip-permissions
```

### Q: メッセージが届かない

```bash
# ログ確認
tail -f logs/send_log.txt
```

### Q: 最初からやり直したい

```bash
tmux kill-server
rm -rf ./tmp/*
./setup.sh
```

### Q: Claude Codeが起動しない

```bash
# バージョン確認
claude --version

# 認証確認（初回はブラウザ認証が必要）
```

---

## tmux基本操作

### セッション操作

| 操作 | コマンド |
|------|---------|
| セッション一覧 | `tmux ls` |
| アタッチ | `tmux a -t 名前` |
| デタッチ | `Ctrl+B` → `D` |
| 終了 | `tmux kill-session -t 名前` |
| 全終了 | `tmux kill-server` |

### ペイン操作

| 操作 | コマンド |
|------|---------|
| ペイン移動 | `Ctrl+B` → `矢印キー` |
| ペインZoom | `Ctrl+B` → `z` |

---

## 参考リンク

- [Claude Code公式](https://docs.anthropic.com/ja/docs/claude-code/overview)
- [tmux チートシート](https://tmuxcheatsheet.com/)
- [GitHub: Claude-Code-Communication](https://github.com/Akira-Papa/Claude-Code-Communication)

---

## 謝辞

- **元木さん** - Claude Code双方向通信の発案者
- **ダイコンさん** - 環境構築のシェア
- **神威/KAMUIさん** - 情報提供

---

Happy Coding with AI Team! 🚀